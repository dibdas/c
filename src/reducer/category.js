import { CHANGE_CATEGORY } from "../actions/constants";
const catergoryreducer=(state=[],action)=>{
    switch (action.type){
    case CHANGE_CATEGORY:
        return state
        default:
        return state

    }
}
export default catergoryreducer